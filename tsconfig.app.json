// tsconfig.app.json
{
  // 1. Extend the configuration generated by Nuxt for the app
  "extends": "./.nuxt/tsconfig.app.json",
  "compilerOptions": {
    // 2. Add the 'node' type definitions to resolve the 'process' global
    "types": [
      "node"
    ]
  }
}